package com.taskanalysis.entity;

/**
 * User roles for authorization
 */
public enum Role {
    /**
     * Regular user with standard access
     */
    USER,
    
    /**
     * Administrator with full access to user management
     */
    ADMIN
}
