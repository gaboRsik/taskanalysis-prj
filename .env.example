# Task Analysis - Production Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to Git (it's in .gitignore)

# ===== DATABASE CONFIGURATION =====
MYSQL_ROOT_PASSWORD=your_strong_root_password_here
MYSQL_DATABASE=taskanalysis
MYSQL_USER=taskanalysis_user
MYSQL_PASSWORD=your_strong_database_password_here

# ===== JWT CONFIGURATION =====
# Generate a strong secret key (minimum 256 bits for HS256)
# Example generation: openssl rand -base64 64
JWT_SECRET=your-jwt-secret-key-minimum-256-bits-required-change-this-in-production
JWT_ACCESS_TOKEN_EXPIRATION=3600000
JWT_REFRESH_TOKEN_EXPIRATION=86400000

# ===== EMAIL CONFIGURATION =====
# Gmail example (use App Password, not your regular password)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-gmail-app-password
MAIL_FROM=Task Analysis <noreply@taskanalysis.com>

# Other SMTP providers:
# - SendGrid: smtp.sendgrid.net (Port 587)
# - Mailgun: smtp.mailgun.org (Port 587)
# - AWS SES: email-smtp.us-east-1.amazonaws.com (Port 587)

# ===== CORS CONFIGURATION =====
# In production, set this to your actual domain
# Multiple origins: http://yourdomain.com,https://yourdomain.com
CORS_ALLOWED_ORIGINS=http://yourdomain.com,https://yourdomain.com

# ===== SPRING PROFILES =====
SPRING_PROFILES_ACTIVE=prod

# ===== OPTIONAL: AWS/CLOUD CONFIGURATION =====
# Uncomment if using cloud services
# AWS_REGION=eu-central-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# ===== INSTRUCTIONS =====
# 1. Copy this file: cp .env.example .env
# 2. Fill in all values with your actual credentials
# 3. Secure the .env file: chmod 600 .env
# 4. NEVER commit .env to version control
